{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Upgrade container image on Dockerfile and build.yaml files.",
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "(^|/)Dockerfile$"
      ],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "ARG BUILD_FROM=(?<depName>.*?):(?<currentValue>[^@\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?\\s+"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "(^|/)build.yaml$"
      ],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "(?aarch64|x86_64|armv7):\\s(?<depName>[^:]+?):(?<currentValue>[^@\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?\\s+"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
